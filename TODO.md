# TODO

## 本轮已完成：Web 功能收敛

- [x] 使用单一日期控件，后端统一拆分为年、月、日，并兼容旧表单字段。
- [x] 使用带缩略图的纸张模板卡片替代“下拉框 + 重复名称标签”。
- [x] Web 端移除生成前的 JPG / PNG 设置，统一在结果区提供 PDF 与图片包下载。
- [x] Web 端移除原始随机种子输入，以“换一种笔迹”表达用户目标。
- [x] 删除重复的步骤编号、顶部功能标签和右侧流程说明。
- [x] 结果区统一操作语义：下载 PDF、下载图片包、换一种笔迹、返回编辑、重置全部。
- [x] 单页下载统一放在大图预览中，删除缩略图卡片上的重复入口。
- [x] 正文优先展示，会议信息明确标记为选填。
- [x] 前后端同步收敛 Web 表单字段；命令行的 `--seed`、`--format` 保持不变。

## Web 后续优化

### Phase W1：模板能力模型（最高优先级）

- [ ] 为纸张预设增加 `capabilities.metadata_fields`，由模板声明支持的会议信息字段。
- [ ] 前端根据模板能力动态显示、隐藏或解释日期、标题、主持人等字段。
- [ ] 重构页面模型，原生支持单面、双面、首页与续页，不再要求单面模板伪造 `back`。
- [ ] 为模板增加说明、适用场景、尺寸和推荐正文长度等元数据。
- [ ] 增加模板配置校验和模板能力回归测试。

### Phase W2：生成任务与真实反馈

- [ ] 将同步生成改为任务式 API，提交后返回不可预测的完整 `job_id`。
- [ ] 通过轮询或 SSE 返回真实阶段、当前页数和错误信息。
- [ ] 支持取消生成、失败重试和重复提交幂等保护。
- [ ] 在生成前估算页数，并对超过最大页数的内容提前预警。
- [ ] 增加低清首页预览，正文停止输入后按防抖策略更新。

### Phase W3：输出、隐私与生命周期

- [ ] 支持基于日期、会议标题的可读文件名。
- [ ] 在结果区按需提供 JPG / PNG 图片包，避免生成前暴露输出实现参数。
- [ ] 为生成结果设置过期时间并定时清理临时图片、PDF 和 ZIP。
- [ ] 输出地址使用更高强度随机标识，并根据部署场景增加授权或签名校验。
- [ ] 为结果响应增加合适的 `Cache-Control`、隐私提示和禁止搜索引擎索引配置。
- [ ] 对磁盘空间、生成失败率和任务耗时增加监控。

### Phase W4：跨设备体验

- [ ] 移动端改为“编辑 / 预览结果”切换，避免长页面来回滚动。
- [ ] 显示“草稿已保存”状态，并支持主动删除草稿。
- [ ] 明确草稿保存范围、有效期和隐私说明。
- [ ] 完成键盘、触屏、屏幕阅读器和高对比度模式测试。

### Phase W5：扩展能力（按需求实施）

- [ ] 提供面向用户的笔迹风格：工整、自然、潦草。
- [ ] 提供书写密度、墨水颜色等语义化设置，不暴露坐标和行距数值。
- [ ] 支持 DOCX / Markdown 输入预处理。
- [ ] 支持 JSON / CSV 导入会议元数据。
- [ ] 评估历史记录、登录、分享和团队协作需求。

## 命令行与排版能力

### 已完成

- [x] 增加 `--check-config`，运行前检查字体、背景图和布局配置。
- [x] 增加 `--debug-box`，绘制元数据区域与正文基线。
- [x] 增加 `--seed`，支持可复现结果和排版回归。
- [x] 增加 PDF 多页合并输出。
- [x] 优化字体、背景图和配置错误提示。
- [x] 支持纸张注册表和 `paper_type` 模板切换。

### 待处理

- [ ] 支持 `meta_apply_rules_fields`，配置哪些元数据字段启用标点禁则。
- [ ] 支持行首、行尾禁则字符集配置。
- [ ] 增加 `--verbose` 日志级别。
- [ ] 增加 `--dump-default-config`。
- [ ] 支持输出文件名模板变量。
- [ ] 增加 `--copies N` 批量随机生成。
- [ ] 支持 TXT / Markdown 清洗、全半角和标点归一化。

## 质量保障

- [ ] 建立基础自动化测试目录和持续集成检查。
- [ ] 覆盖行首、行尾禁则以及 `——`、`……` 不拆分测试。
- [ ] 覆盖分页、最大页数、元数据截断和无背景图兜底测试。
- [ ] 覆盖 Web 日期转换、模板选择、AJAX 校验和 ZIP 下载测试。
- [ ] 增加桌面与移动端关键页面的视觉回归测试。
