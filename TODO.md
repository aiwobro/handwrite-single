# TODO

## 建议实施顺序

### Phase 1（优先做：高收益、低风险）

1. [x] 增加 `--check-config`（避免运行期报错）
2. [x] 增加 `--debug-box`（显著降低坐标调试成本）
3. [x] 增加 `--seed`（结果可复现，便于排查与回归）
4. [x] 优化错误提示（减少排障时间）

### Phase 2（可配置能力）

1. [ ] 支持 `meta_apply_rules_fields`（元数据字段规则可配置）
2. [ ] 支持禁则字符集配置（适配不同规范）
3. [ ] 增加 `--verbose` 日志级别（增强可观测性）

### Phase 3（通用输入与模板生态）

1. [ ] 支持输入预处理（TXT/Markdown 清洗与归一）
2. [ ] 支持 `JSON/CSV` 导入元数据
3. [ ] 支持模板目录与模板切换
4. [ ] 增加 `--dump-default-config`

### Phase 4（输出增强与工程化）

1. [ ] 增加 `PDF` 输出（多页合并）
2. [ ] 支持输出文件名模板变量
3. [ ] 增加 `--copies N` 批量生成
4. [ ] 增加断行规则与分页回归测试

## 易用性

- [x] 增加 `--check-config`：运行前校验字体文件、背景图、坐标范围、字段完整性。
- [ ] 增加 `--dump-default-config`：一键导出默认配置模板，降低首次使用成本。
- [x] 增加 `--debug-box`：在输出图中绘制 `meta_position` 矩形框与行基线，便于调坐标。
- [ ] 增加 `--verbose` 日志级别：输出更详细的排版与分页过程信息。
- [x] 优化错误提示：缺字体/缺背景图/配置错误时，给出可操作的修复建议。

## 通用性

- [ ] 支持 `meta` 字段规则配置（如 `meta_apply_rules_fields`），控制哪些字段启用标点禁则断行。
- [ ] 支持禁则字符集配置（行首禁则/行尾禁则），适配不同单位规范。
- [ ] 支持输入预处理（TXT/Markdown）：清理多余空行、统一全半角、常见标点归一化。
- [ ] 支持从 `JSON/CSV` 导入元数据，便于与外部系统对接。
- [ ] 支持模板目录与模板切换（会议纪要、课堂笔记、信纸等），并可绑定默认版式与字体组。

## 输出与批处理

- [ ] 增加 `PDF` 输出（多页合并）。
- [ ] 支持输出文件名模板变量（日期、会议标题、前缀等）。
- [x] 增加 `--seed`：支持随机笔迹可复现。
- [ ] 增加 `--copies N`：一次生成多份随机版本。

## 质量保障

- [ ] 增加最小测试集，覆盖断行规则（行首/行尾禁则、`——`、`……` 不拆分）。
- [ ] 增加基础回归测试（分页、元数据框截断、无背景图兜底）。
